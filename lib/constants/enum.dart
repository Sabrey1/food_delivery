// ignore_for_file: constant_identifier_names

enum StorageKey {
  workspaces,
  currentLanguage,
  currentWorkspace,
  isBackToWorkspace,
  allowMultipleSite,
  rawCookie,
  userLogon,
  currentPOSProfile,
  systemMode,
  appStartUp,
}

enum ENV { serverUrl, aesIv, aesKey }

enum SystemMode { system, light, dark }
