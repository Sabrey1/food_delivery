import 'package:flutter/material.dart';

class ScrollToTopScreen extends StatelessWidget {
  const ScrollToTopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}