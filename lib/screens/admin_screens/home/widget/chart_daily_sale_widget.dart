import 'package:flutter/material.dart';

class ChartDailySaleWidget extends StatelessWidget {
  const ChartDailySaleWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Chart Daily Sale"),),
    );
  }
}